export default function Friends() {
  return <div className="page-container">Voici la liste de tes amis
  {/* <div className="mt-4">
            <h2>Visiter un autre profil :</h2>
            <input
              type="text"
              placeholder="Nom d'utilisateur"
              value={username}
              onChange={(e) => setUsername(e.target.value)}
              className="border p-2 mb-2 block"
            />
            <button onClick={() => setSelectedUsername(username)} className="bg-gray-500 text-white px-4 py-2 rounded">
              Voir le profil
            </button>
          </div> */}
  </div>;
}
